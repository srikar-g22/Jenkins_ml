pipeline {
    agent any
    tools {
        dockerTool 'docker_jenkins'
        dockerTool 'python:3.8-slim-buster'
    }
    stages {
        stage('Setup') {
            steps {
                sh 'pip3 install -r requirements.txt'
            }
        }
        stage('Train') {
            steps {
                sh 'python trainingML.py'
            }
        }  
    }
}

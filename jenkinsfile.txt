pipeline {
    agent any
    tools {
        dockerTool 'docker_jenkins'
        docker_jenkins 'python:3.8-slim'
    }
    stages {
        stage('Setup') {
            steps {
                sh 'pip3 install -r requirements.txt'
            }
        }
        stage('Train') {
            steps {
                sh 'python trainingML.py'
            }
        }  
    }
}
